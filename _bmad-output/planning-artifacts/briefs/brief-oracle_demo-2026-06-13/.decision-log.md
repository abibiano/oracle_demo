# Decision Log — oracle_demo brief

Created 2026-06-13 · intent: create · mode: Fast path

## Decisions

- **2026-06-13** — Product framed as a *reference/showcase example app* for the author's own `oracledb` v1.0.0 pure-Dart driver, not a standalone product. Source: user brain dump + discovery (package authored by user at /Users/abibiano/Projects/Flutter/dart-oracledb).
- **2026-06-13** — Architecture to mirror `pixcontest/concurs`: data/domain/presentation + application layer, Riverpod code-gen (flutter_riverpod, hooks_riverpod, riverpod_annotation, riverpod_generator), Freezed, Result<T,AppFailure>, repository pattern, FutureProvider.family + local keepAlive page-index provider for pagination, flutter_dotenv config. Source: Explore of concurs project.
- **2026-06-13** — No API backend; direct Oracle connection. Explicit user requirement.
- **2026-06-13** — Target macOS desktop. Rationale: user requested macOS; `oracledb` does not support web. Marked [ASSUMPTION] that v1 is macOS-only.

## Confirmed 2026-06-13 (user via AskUserQuestion)

- Connection management: **OraclePool** (recommended).
- Platform: **macOS-only** for v1.
- Scope: **read-only** (no writes/PLSQL/sort/filter in v1).
- Pagination: **SQL OFFSET/FETCH + COUNT(*)**.
- Routing: **stub go_router** for concurs fidelity.

## Resolved 2026-06-13 (user-provided detail)

- Target table: **`cliente`** (CRM clients), specific column list + ORDER BY alta_cli DESC, potencial, nomb_cli. Captured in addendum.md.
- Connection: `192.168.2.30:1521/CELO` (TNS SERVICE_NAME=CELO), user `NIK`, password in `.env`. EZ Connect: `192.168.2.30:1521/CELO`.
- Mandatory per-session init PL/SQL block (filial/usuario_id/idioma) → run via OraclePool `sessionCallback`. Captured in addendum.md; surfaced in brief as showcased feature.

## Addendum handling

- Exact SQL, column→model mapping, connection descriptor, session-init block → addendum.md (downstream PRD/architecture/dev detail).
- Session-init *requirement* + sessionCallback mapping → also surfaced in brief (Solution + What Makes This Different + Scope) as a teaching highlight.

- **2026-06-13** — UI component library: **`shadcn_ui`** instead of Material. Deliberate divergence from concurs (which uses Material + flex_color_scheme); architecture/state layers still mirror concurs. Rationale: user wants to try shadcn-style components.

- **2026-06-13** — Table rendering: **`ShadTable` + `ShadButton`-composed pagination**, no third-party grid. Rationale: SQL-side paging renders only one small page, which fits ShadTable's small-table design; keeps the UI fully shadcn-styled. Trina Grid / Syncfusion DataGrid rejected for v1 (render unthemed widgets). Captured in addendum.md.

## No open items remaining (dev-time only: confirm column Dart types, dedupe column list).

## Overrides

(none)
